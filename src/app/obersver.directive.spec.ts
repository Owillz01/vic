import { ObersverDirective } from './obersver.directive';

describe('ObersverDirective', () => {
  it('should create an instance', () => {
    const directive = new ObersverDirective();
    expect(directive).toBeTruthy();
  });
});
